/** Single source of truth — keep in sync with app.json android.versionCode. */
export const APP_VERSION = '13.2.1';
export const APP_VERSION_CODE = 16;
export const APP_VERSION_DISPLAY = `${APP_VERSION} (${APP_VERSION_CODE})`;
